<UserControl x:Class="AlhimikGame.WPF.Views.NpcInteractionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:AlhimikGame.WPF.ViewModels"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="600">
    <UserControl.Resources>
        <!-- Colors -->
        <SolidColorBrush x:Key="BackgroundColor" Color="#1C1D2E"/>
        <SolidColorBrush x:Key="PanelColor" Color="#252638"/>
        <SolidColorBrush x:Key="AccentColor" Color="#8A7CDB"/>
        <SolidColorBrush x:Key="TextColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SecondaryTextColor" Color="#B0B0B0"/>
        <SolidColorBrush x:Key="ButtonColor" Color="#8A7CDB"/>
        <SolidColorBrush x:Key="ButtonHoverColor" Color="#A094E3"/>
        
        <!-- Button Style -->
        <Style x:Key="PurpleButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource ButtonColor}"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource ButtonHoverColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Radio Button Style -->
        <Style x:Key="CustomRadioButton" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="RadioBorder" 
                                    Width="18" Height="18" 
                                    BorderBrush="{StaticResource AccentColor}" 
                                    BorderThickness="1" 
                                    CornerRadius="9">
                                <Ellipse x:Name="RadioMark" 
                                         Width="10" Height="10" 
                                         Fill="{StaticResource AccentColor}" 
                                         Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter Grid.Column="1" 
                                              Margin="10,0,0,0" 
                                              VerticalAlignment="Center" 
                                              Content="{TemplateBinding Content}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="RadioMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid Background="{StaticResource BackgroundColor}">
        <Border Background="{StaticResource PanelColor}" 
                Margin="20" 
                CornerRadius="10"
                BorderThickness="1"
                BorderBrush="#40FFFFFF">
            <ContentControl Content="{Binding CurrentContent}" Margin="20">
                <ContentControl.Resources>
                    <!-- Greeting Template -->
                    <DataTemplate DataType="{x:Type viewModels:GreetingContent}">
                        <StackPanel>
                            <TextBlock Text="{Binding NpcName}" 
                                   FontSize="24" 
                                   FontWeight="Bold" 
                                   Margin="0,0,0,20"
                                   Foreground="{StaticResource TextColor}"/>
                            
                            <Border Background="#30FFFFFF" Padding="15" CornerRadius="8">
                                <TextBlock Text="{Binding GreetingText}" 
                                       TextWrapping="Wrap"
                                       FontSize="16"
                                       Foreground="{StaticResource TextColor}"/>
                            </Border>
                            
                            <Button Content="Start Quiz" 
                                Command="{Binding StartCommand}"
                                Margin="0,20,0,0"
                                Style="{StaticResource PurpleButton}"
                                HorizontalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                    
                    <!-- Question Template -->
                    <DataTemplate DataType="{x:Type viewModels:QuestionContent}">
                        <StackPanel>
                            <TextBlock Text="{Binding QuestionText}" 
                                   TextWrapping="Wrap"
                                   FontSize="18"
                                   Margin="0,0,0,20"
                                   Foreground="{StaticResource TextColor}"/>
                            
                            <ItemsControl ItemsSource="{Binding Answers}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <RadioButton Content="{Binding Text}" 
                                                 GroupName="Answers"
                                                 Style="{StaticResource CustomRadioButton}"
                                                 IsChecked="{Binding IsSelected}"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            
                            <Button Content="Submit Answer" 
                                Command="{Binding SubmitCommand}"
                                Margin="0,20,0,0"
                                Style="{StaticResource PurpleButton}"
                                HorizontalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                    
                    <!-- Result Template -->
                    <DataTemplate DataType="{x:Type viewModels:ResultContent}">
                        <StackPanel>
                            <Border Background="#30FFFFFF" Padding="15" CornerRadius="8">
                                <TextBlock Text="{Binding ResultText}" 
                                       TextWrapping="Wrap"
                                       FontSize="16"
                                       Foreground="{StaticResource TextColor}"/>
                            </Border>
                            
                            <Button Content="{Binding NextButtonText}" 
                                Command="{Binding NextCommand}"
                                Margin="0,20,0,0"
                                Style="{StaticResource PurpleButton}"
                                HorizontalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </ContentControl.Resources>
            </ContentControl>
        </Border>
    </Grid>
</UserControl>